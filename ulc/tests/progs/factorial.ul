/*
 * Recursive factorial:
 *  read N and print N!, using a recursive algorithm
 */

factorial(data n) {
	if (n == 0)
		return 1;
	return n * factorial(n - 1);
}

main {
	data n = read;
	write factorial(n);
}
