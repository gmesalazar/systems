/* 
 * Fibonacci:
 *  read N and print the fibonacci sequence up to N
 */
main {
	number prim = 0, seg = 1, next, max;
	read max;
	while (prim <= max) {
		write prim;
		next = prim + seg;
		prim = seg;
		seg = next;
	}
}
