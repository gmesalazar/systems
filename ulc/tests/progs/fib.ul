/* 
 * Fibonacci:
 *  read N and print the fibonacci sequence up to N
 */
main {
	data prim = 0, seg = 1, next, max = read;
	while (prim <= max) {
		write prim;
		next = prim + seg;
		prim = seg;
		seg = next;
	}
}
